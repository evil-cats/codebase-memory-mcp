# Сборка и установка

- Собирай production-бинарник командой `scripts/build.sh`. По умолчанию скрипт
  определяет версию из текущей ветки выпуска: `vX.Y.Z-lora` превращается в
  `vX.Y.Z`; для обычной сборки не передавай `--version`.
- Устанавливай собранный бинарник командой `scripts/install.sh`.
